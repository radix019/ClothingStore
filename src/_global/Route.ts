export enum CATEGORY_PARAM_TYPE {
  CATEGORY = "category",
}

export enum PAGE_TYPE {
  AUTH = "auth",
  SIGN_UP = "signup",
  CHECKOUT = "checkout",
  SHOP = "shop",
}

export enum SCREEN_TYPE {
  MAIN = "main",
}
